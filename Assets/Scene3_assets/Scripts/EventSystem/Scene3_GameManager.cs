using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Scene3_GameManager : MonoBehaviour
{
    // private Scene3_PhoneManager Phone;
    // private Scene3_BackpackManager Backpack;
    // Start is called before the first frame update
    void Start()
    {
        // Find functions 
        // Phone = FindObjectOfType<Scene3_PhoneManager>();
        // Backpack = FindObjectOfType<Scene3_BackpackManager>();

        // Init phone stage
        // Phone.phoneStage(3);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
